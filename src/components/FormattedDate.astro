---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-normal text-gray-500 font-merri">
	{
		date.toLocaleDateString("de-DE", {
			// weekday: "short",
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>


<h1 class="font-budmo1 text-5xl">FORRO DA CAPITA</h1>

<style>
  @property --k {
    syntax: "<number>";
    initial-value: 0;
    inherits: false;
  }

  h1:hover {
    --k: 0;
    background: linear-gradient(
        90deg,
        hsl(calc(var(--k) * 1turn), 95%, 65%),
        hsl(calc(var(--k) * 1turn + 90deg), 95%, 65%)
      )
      text;
    color: transparent;
    filter: url(#f);
    text-transform: uppercase;
    animation: k 6s linear infinite alternate;
  }

  @keyframes k {
    to {
      --k: 1;
    }
  }
</style>
